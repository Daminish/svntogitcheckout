/*package com.capco.hcm.controller.impl;

import javax.faces.bean.ManagedBean;
import javax.faces.bean.ManagedProperty;

import org.primefaces.event.TabChangeEvent;

import com.capco.hcm.view.fragments.BenefitsTravelTrainingView;

@ManagedBean(name="tabChangeEvtCtrl")
public class TabChangeEventController {

	private String tabIndex;
	@ManagedProperty("#{beneTrvlTrainView}")
	private BenefitsTravelTrainingView bttView;
	
	public void onTabChange(TabChangeEvent event){
		if(event.getTab().getTitle().equals("Bond, Skill & Training")){
			bttView.loadDefValues();//TODO call some method to load values
			//Method should take employee id as the input parameter and call the dao to load the value
			
		
		}
	}
}
*/