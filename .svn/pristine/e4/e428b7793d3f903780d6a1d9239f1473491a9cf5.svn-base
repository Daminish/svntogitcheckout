package com.capco.travel.dao.impl.test;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.transaction.TransactionConfiguration;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.util.Assert;

import com.capco.travel.dao.impl.EmployeeDAOImpl;
import com.capco.travel.model.EmployeeDetailsBO;

/*@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations={"classpath:test-context.xml",
"classpath:/META-INF/spring/applicationContext.xml"})
@Transactional
@TransactionConfiguration(defaultRollback=true)
public class EmployeeDaoImplTest {
	
	    @Autowired
	    private EmployeeDaoImpl employeeDaoImpl;

	    @Test
	    public void testFindById()
	    {
	        EmployeeDetailsBO emp = employeeDaoImpl.getEmployeeDetails(16484);

	        Assert.assertEquals("Sonali", emp.getEmployeeName());
	        Assert.assertEquals("john.smith@mailinator.com", emp.getEmailId());
	        Assert.assertEquals("2125551212", emp.getPhoneNumber());
	        return;
	    }

}*/
